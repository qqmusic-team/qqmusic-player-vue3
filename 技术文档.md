# QQ 音乐播放器 - 技术文档

## 1. 项目概述和目标

### 1.1 项目简介

QQ 音乐播放器是一款基于 Vue3 开发的现代化音乐播放应用，旨在为用户提供高质量的音乐播放体验。该应用集成了音乐推荐、音乐馆浏览、精选评论等核心功能，并支持响应式设计和平滑的动画过渡效果。

### 1.2 主要功能

- 音乐推荐系统：根据用户偏好推荐个性化音乐内容
- 音乐馆浏览：提供分类音乐内容浏览和搜索
- 精选评论：展示和管理音乐评论
- 个人中心：用户个人信息和播放历史管理
- 音乐播放控制：播放、暂停、上一曲、下一曲等基本控制功能
- 响应式设计：适配不同屏幕尺寸的设备
- 深色模式支持：提供浅色/深色主题切换功能

### 1.3 项目目标

- 提供流畅、直观的用户界面和交互体验
- 构建高性能、可扩展的前端架构
- 实现完整的音乐播放功能生态
- 确保代码质量和可维护性
- 支持跨设备兼容和用户体验一致性

### 1.4 技术栈

- 前端框架：Vue 3
- 路由管理：Vue Router
- 状态管理：Vuex/Pinia（根据项目实际使用）
- UI 组件：自定义组件
- CSS 预处理器：（根据项目实际使用）
- 构建工具：Vite

## 2. 系统架构和组件关系

### 2.1 整体架构

QQ 音乐播放器采用典型的 Vue3 单页应用(SPA)架构，基于组件化开发思想构建。应用整体遵循清晰的分层设计，包括视图层、组件层、路由层和服务层。

```
┌─────────────────────────────────────────────┐
│                 应用层 (App.vue)            │
└───────────────────────────┬─────────┘
                                    │
┌───────────────────────┼─────────┐
│                路由层 (Vue Router)         │
└───────────────────────────────┼─────────┘
                                    │
┌─────────────────────┬─────────────┼─────────┬─────────────────────┐
│                     │             │         │                     │
│   HomeView          │  其他视图   │  Sidebar│     TopBar          │
│  (主布局组件)       │  组件       │(侧边栏) │    (顶部导航)       │
└────────┬────────────┴─────────────┴─────────┴────────┬───────────┘
         │                                             │
┌────────┴─────────────────┬───────────────────────────┴────────┐
│                          │                                   │
│  动态页面组件            │       PlayerBar (播放器控制栏)       │
│ (RecommendView等)       │                                   │
└──────────────────────────┴───────────────────────────────────┘
```

### 2.2 核心组件结构

#### 2.2.1 布局组件

- **App.vue**：应用根组件，负责挂载主视图组件
- **HomeView.vue**：主布局视图，包含侧边栏、顶部导航和动态内容区域，实现了自定义导航系统
- **Sidebar.vue**：侧边栏组件，提供主要导航功能和菜单项目
- **TopBar.vue**：顶部导航栏，包含搜索功能、用户信息和其他辅助功能
- **PlayerBar.vue**：底部播放器控制栏，提供音乐播放控制功能

#### 2.2.2 页面组件

- **RecommendView.vue**：推荐页面，展示个性化音乐推荐内容
- **MusicHallView.vue**：音乐馆页面，提供音乐分类浏览功能
- **RankView.vue**：排行榜页面，展示音乐榜单信息
- **ProfileView.vue**：个人主页，展示用户个人信息和设置
- **FavoritesView.vue**：我喜欢页面，展示用户收藏的音乐
- **LocalSongsView.vue**：本地歌曲页面，管理本地音乐文件
- **DownloadsView.vue**：下载歌曲页面，管理已下载的音乐
- **RecentPlaysView.vue**：最近播放页面，展示最近播放历史

### 2.3 导航系统设计

项目采用了自定义的导航系统，在 HomeView 组件中实现了动态组件加载机制，而非传统的嵌套 router-view。这种设计提供了更灵活的页面切换控制和过渡效果。

#### 2.3.1 导航工作流程

1. 用户点击 Sidebar 中的导航项
2. Vue Router 更新路由路径
3. HomeView 组件监听路由变化（`$route.path`）
4. 根据当前路由路径，动态加载对应的页面组件
5. 通过 Vue 的 Transition 组件实现页面切换动画

#### 2.3.2 导航状态管理

- 使用 Vue Router 的路由状态管理当前活动页面
- Sidebar 组件中的 activeItem 状态控制导航项的高亮显示
- HomeView 组件维护页面加载状态和错误处理

### 2.4 数据流

- **组件间通信**：主要通过 Props 和 Events 进行父子组件通信
- **全局状态**：使用 Vuex/Pinia 管理全局状态（如播放器状态、用户信息等）
- **路由参数**：通过 Vue Router 传递页面参数和状态

### 2.5 关键交互流程

#### 2.5.1 页面切换流程

1. 用户点击导航菜单
2. 更新路由
3. HomeView 检测路由变化
4. 卸载当前页面组件
5. 执行页面过渡动画
6. 加载新页面组件
7. 更新导航高亮状态

#### 2.5.2 音乐播放流程

1. 用户选择音乐
2. 更新全局播放器状态
3. PlayerBar 组件接收播放状态更新
4. 执行播放动画和 UI 更新

## 3. 环境设置指南

### 3.1 先决条件

在开始开发前，请确保您的系统已安装以下软件：

#### 3.1.1 必要工具

- **Node.js**：v14.x 或更高版本
  - 下载地址：[https://nodejs.org/](https://nodejs.org/)
  - 建议使用 Node.js v16.x LTS 版本以获得最佳兼容性
- **npm**：v6.x 或更高版本（随 Node.js 一起安装）
  - 或使用 **yarn**：v1.22.x 或更高版本
- **Git**：用于版本控制
  - 下载地址：[https://git-scm.com/](https://git-scm.com/)

#### 3.1.2 开发环境推荐

- **编辑器**：Visual Studio Code
  - 推荐插件：
    - Volar（Vue 3 支持）
    - ESLint
    - Prettier - Code formatter
    - GitLens
- **浏览器**：Chrome 或 Firefox 最新版
  - 推荐插件：Vue DevTools

### 3.2 项目安装步骤

#### 3.2.1 克隆项目仓库

```bash
# 克隆项目
cd d:\00html_project\真前端大作业\真自己的大作业

# 注意：如果项目已存在，请跳过此步骤
```

#### 3.2.2 安装依赖

进入项目目录后，执行以下命令安装项目依赖：

```bash
# 使用 npm
npm install

# 或使用 yarn
yarn install
```

安装过程中可能出现的依赖冲突或警告，请根据提示解决。

#### 3.2.3 开发服务器启动

```bash
# 使用 npm
npm run dev

# 或使用 yarn
yarn dev
```

执行后，开发服务器将在本地启动，通常默认访问地址为：[http://localhost:3000](http://localhost:3000)

#### 3.2.4 构建项目

```bash
# 使用 npm
npm run build

# 或使用 yarn
yarn build
```

构建后的文件将位于 `dist` 目录中。

#### 3.2.5 预览构建结果

```bash
# 使用 npm
npm run preview

# 或使用 yarn
yarn preview
```

### 3.3 配置要求

#### 3.3.1 项目配置文件

- **vite.config.js**：Vite 构建工具配置
  - 可自定义开发服务器端口、代理设置等
- **package.json**：项目依赖和脚本定义
- **.eslintrc.js**：ESLint 代码检查配置
- **.prettierrc**：Prettier 代码格式化配置

#### 3.3.2 开发环境配置

##### 3.3.2.1 环境变量

项目支持以下环境变量配置：

- **.env**：所有环境通用配置
- **.env.development**：开发环境配置
- **.env.production**：生产环境配置

常用环境变量示例：

```
# API 基础 URL
VITE_API_BASE_URL=http://localhost:8080/api

# 是否启用模拟数据
VITE_USE_MOCK=true

# 开发服务器端口
VITE_PORT=3000
```

#### 3.3.3 代理设置

如果需要配置 API 代理，可在 vite.config.js 中修改：

```javascript
// vite.config.js 示例配置
export default defineConfig({
  server: {
    proxy: {
      "/api": {
        target: "http://localhost:8080",
        changeOrigin: true,
        rewrite: (path) => path.replace(/^\/api/, ""),
      },
    },
  },
});
```

### 3.4 常见问题解决

#### 3.4.1 依赖安装失败

- 检查 Node.js 版本是否兼容
- 尝试清除 npm 缓存：`npm cache clean --force`
- 尝试使用 Yarn 替代 npm

#### 3.4.2 开发服务器启动失败

- 检查端口是否被占用，可在配置文件中修改端口
- 检查 vite.config.js 配置是否正确
- 查看错误日志获取详细信息

#### 3.4.3 构建失败

- 检查代码是否存在语法错误
- 确保所有依赖都已正确安装
- 查看构建日志获取详细错误信息

## 4. 开发工作流程指南

### 4.1 分支管理策略

项目采用 Git Flow 分支管理模式，以下是主要分支的说明和用途：

#### 4.1.1 主要分支

- **main/master**：主分支，包含生产环境的稳定代码

  - 受保护分支，不允许直接提交
  - 只能通过 Pull Request 从 release 分支合并
  - 每次合并到 main 分支需要进行代码审查

- **develop**：开发分支，包含最新的开发代码
  - 受保护分支，不允许直接提交
  - 从 feature 分支和 hotfix 分支合并
  - 作为新功能开发的基础分支

#### 4.1.2 辅助分支

- **feature/xxx**：功能分支，用于开发新功能

  - 命名格式：`feature/功能名称`（使用小写字母和连字符）
  - 从 develop 分支创建
  - 完成开发后合并回 develop 分支
  - 功能完成并合并后删除

- **release/xxx**：发布分支，用于版本发布准备

  - 命名格式：`release/v版本号`（例如：`release/v1.0.0`）
  - 从 develop 分支创建
  - 进行发布前的最终测试和修复
  - 完成后同时合并到 main 和 develop 分支
  - 发布完成后删除

- **hotfix/xxx**：热修复分支，用于修复生产环境紧急问题
  - 命名格式：`hotfix/问题描述`（使用小写字母和连字符）
  - 从 main 分支创建
  - 修复完成后同时合并到 main 和 develop 分支
  - 修复完成后删除

### 4.2 提交规范

为了保持代码仓库的整洁和可追踪性，请遵循以下提交信息规范：

#### 4.2.1 提交信息格式

```
<类型>[(可选范围)]: <描述>

[可选的正文]

[可选的脚注]
```

#### 4.2.2 提交类型

- **feat**：新增功能
- **fix**：修复错误
- **docs**：文档更新
- **style**：代码风格修改（不影响代码逻辑）
- **refactor**：代码重构（既不修复错误也不添加功能）
- **perf**：性能优化
- **test**：添加或修改测试
- **build**：构建系统或外部依赖项修改
- **ci**：CI 配置修改
- **chore**：其他修改（不涉及源代码或测试文件）
- **revert**：撤销先前的提交

#### 4.2.3 提交信息示例

```
feat(sidebar): 添加夜间模式切换按钮

添加了侧边栏中的主题切换功能，支持亮色/暗色模式一键切换

Closes #123
```

```
fix(player): 修复播放暂停按钮在某些浏览器下不响应的问题

优化了按钮事件处理逻辑，确保在所有主流浏览器中正常工作
```

#### 4.2.4 提交规范最佳实践

- 保持提交信息简洁明了（不超过 72 个字符）
- 使用现在时态（"add feature" 而非 "added feature"）
- 使用祈使语气（"update dependencies" 而非 "updates dependencies"）
- 对于重大修改，在正文中提供更多详细信息
- 引用相关的 Issue 或 Pull Request

### 4.3 日常开发流程

#### 4.3.1 开始新功能开发

1. 确保本地 develop 分支是最新的：

   ```bash
   git checkout develop
   git pull origin develop
   ```

2. 创建新的 feature 分支：

   ```bash
   git checkout -b feature/new-feature-name
   ```

3. 进行开发工作

4. 定期提交更改：

   ```bash
   git add .
   git commit -m "feat(模块): 功能描述"
   ```

5. 定期从 develop 分支拉取更新，避免冲突：
   ```bash
   git pull origin develop
   ```

#### 4.3.2 完成功能开发

1. 确保所有测试通过

2. 将 feature 分支推送到远程仓库：

   ```bash
   git push origin feature/new-feature-name
   ```

3. 在代码托管平台（如 GitHub、GitLab）创建 Pull Request 到 develop 分支

4. 等待代码审查和批准

5. Pull Request 被合并后，删除本地和远程的 feature 分支：
   ```bash
   git checkout develop
   git branch -d feature/new-feature-name
   git push origin --delete feature/new-feature-name
   ```

#### 4.3.3 发布新版本

1. 创建 release 分支：

   ```bash
   git checkout develop
   git pull origin develop
   git checkout -b release/v1.0.0
   ```

2. 更新版本号和变更日志

3. 进行最终测试和修复

4. 将 release 分支推送到远程仓库：

   ```bash
   git push origin release/v1.0.0
   ```

5. 创建 Pull Request 到 main 和 develop 分支

6. 等待代码审查和批准

7. 在 main 分支上创建发布标签：

   ```bash
   git checkout main
   git pull origin main
   git tag v1.0.0
   git push origin v1.0.0
   ```

8. 删除本地和远程的 release 分支

#### 4.3.4 修复生产环境问题

1. 创建 hotfix 分支：

   ```bash
   git checkout main
   git pull origin main
   git checkout -b hotfix/critical-bug-fix
   ```

2. 进行修复工作

3. 测试修复

4. 将 hotfix 分支推送到远程仓库：

   ```bash
   git push origin hotfix/critical-bug-fix
   ```

5. 创建 Pull Request 到 main 和 develop 分支

6. 等待代码审查和批准

7. 在 main 分支上创建修复版本标签：

   ```bash
   git checkout main
   git pull origin main
   git tag v1.0.1
   git push origin v1.0.1
   ```

8. 删除本地和远程的 hotfix 分支

### 4.4 代码审查流程

所有代码变更必须经过代码审查才能合并到受保护分支。

#### 4.4.1 提交代码审查

1. 创建 Pull Request
2. 提供清晰的描述，包括变更内容和目的
3. 关联相关的 Issue
4. 指定至少一名审查者

#### 4.4.2 审查者职责

1. 检查代码是否符合项目的代码风格和最佳实践
2. 验证功能是否按预期工作
3. 检查是否有潜在的性能问题或错误
4. 提供建设性的反馈
5. 批准或请求修改

#### 4.4.3 处理反馈

1. 根据审查者的反馈进行修改
2. 提交修改并更新 Pull Request
3. 通知审查者再次审查

## 5. API 规范文档

### 5.1 API 基础信息

#### 5.1.1 API 基础 URL

```
# 开发环境
VITE_API_BASE_URL=http://localhost:8080/api

# 生产环境
VITE_API_BASE_URL=https://api.qqmusicplayer.com/v1
```

#### 5.1.2 请求方法

API 支持以下 HTTP 请求方法：

- **GET**：获取资源
- **POST**：创建资源
- **PUT**：更新资源
- **DELETE**：删除资源

#### 5.1.3 请求/响应格式

- **请求体**：JSON 格式
- **响应体**：JSON 格式
- **字符编码**：UTF-8

#### 5.1.4 通用响应结构

所有 API 响应遵循统一的结构：

```json
{
  "code": 0, // 状态码，0 表示成功，非 0 表示错误
  "message": "success", // 响应消息
  "data": {
    // 响应数据
    // 具体数据内容
  },
  "timestamp": 1634567890 // 响应时间戳
}
```

### 5.2 认证机制

#### 5.2.1 认证方式

项目使用 JWT（JSON Web Token）进行身份认证。

#### 5.2.2 认证流程

1. 用户登录获取 token
2. 在后续请求的 HTTP Header 中添加 Authorization 头

```
Authorization: Bearer <token>
```

#### 5.2.3 登录接口

##### 5.2.3.1 用户登录

**请求**：

- URL: `/auth/login`
- 方法: `POST`
- 数据类型: `application/json`

**请求体**：

```json
{
  "username": "user123",
  "password": "password123"
}
```

**响应**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "userInfo": {
      "id": 1,
      "username": "user123",
      "nickname": "音乐爱好者",
      "avatar": "https://example.com/avatar.jpg"
    },
    "expiresIn": 86400 // token 过期时间（秒）
  }
}
```

#### 5.2.4 登出接口

##### 5.2.4.1 用户登出

**请求**：

- URL: `/auth/logout`
- 方法: `POST`
- 数据类型: `application/json`
- 认证: 需要 JWT token

**响应**：

```json
{
  "code": 0,
  "message": "success"
}
```

### 5.3 音乐相关 API

#### 5.3.1 获取推荐音乐

##### 5.3.1.1 获取个性化推荐列表

**请求**：

- URL: `/music/recommendations`
- 方法: `GET`
- 认证: 可选（未登录返回通用推荐，登录返回个性化推荐）

**查询参数**：

```
limit: 每页数量，默认 20
offset: 偏移量，默认 0
```

**响应**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "songs": [
      {
        "id": 1,
        "title": "歌曲标题",
        "artist": "艺术家",
        "album": "专辑名称",
        "coverUrl": "https://example.com/cover.jpg",
        "duration": 240, // 秒
        "playCount": 10000
      }
      // 更多歌曲...
    ],
    "total": 100, // 总数
    "hasMore": true // 是否有更多
  }
}
```

#### 5.3.2 获取歌曲详情

##### 5.3.2.1 获取单曲详情

**请求**：

- URL: `/music/songs/:id`
- 方法: `GET`
- 认证: 可选

**路径参数**：

```
id: 歌曲 ID
```

**响应**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "title": "歌曲标题",
    "artist": "艺术家",
    "album": "专辑名称",
    "coverUrl": "https://example.com/cover.jpg",
    "audioUrl": "https://example.com/audio.mp3",
    "duration": 240,
    "lyrics": "歌词内容...",
    "playCount": 10000,
    "releaseDate": "2022-01-01"
  }
}
```

#### 5.3.3 获取播放链接

##### 5.3.3.1 获取歌曲播放链接

**请求**：

- URL: `/music/songs/:id/url`
- 方法: `GET`
- 认证: 需要 JWT token

**路径参数**：

```
id: 歌曲 ID
```

**查询参数**：

```
quality: 音质，可选值：standard（标准）, high（高品）, lossless（无损），默认 standard
```

**响应**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 1,
    "url": "https://example.com/audio-highquality.mp3",
    "quality": "high",
    "expiresIn": 1800 // 链接过期时间（秒）
  }
}
```

### 5.4 音乐馆 API

#### 5.4.1 获取音乐分类

##### 5.4.1.1 获取音乐分类列表

**请求**：

- URL: `/music/categories`
- 方法: `GET`
- 认证: 不需要

**响应**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "categories": [
      {
        "id": 1,
        "name": "流行音乐",
        "icon": "pop"
      },
      {
        "id": 2,
        "name": "摇滚音乐",
        "icon": "rock"
      }
      // 更多分类...
    ]
  }
}
```

#### 5.4.2 获取分类音乐

##### 5.4.2.1 获取指定分类的音乐列表

**请求**：

- URL: `/music/categories/:categoryId/songs`
- 方法: `GET`
- 认证: 不需要

**路径参数**：

```
categoryId: 分类 ID
```

**查询参数**：

```
limit: 每页数量，默认 20
offset: 偏移量，默认 0
```

**响应**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "songs": [
      {
        "id": 1,
        "title": "歌曲标题",
        "artist": "艺术家",
        "album": "专辑名称",
        "coverUrl": "https://example.com/cover.jpg",
        "duration": 240,
        "playCount": 10000
      }
      // 更多歌曲...
    ],
    "total": 50,
    "hasMore": true
  }
}
```

### 5.5 评论 API

#### 5.5.1 获取歌曲评论

##### 5.5.1.1 获取歌曲评论列表

**请求**：

- URL: `/comments/songs/:songId`
- 方法: `GET`
- 认证: 不需要

**路径参数**：

```
songId: 歌曲 ID
```

**查询参数**：

```
limit: 每页数量，默认 20
offset: 偏移量，默认 0
```

**响应**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "comments": [
      {
        "id": 1,
        "content": "很好听的一首歌！",
        "userId": 123,
        "username": "用户123",
        "avatar": "https://example.com/avatar.jpg",
        "createdAt": "2022-01-01T12:00:00Z",
        "likesCount": 50
      }
      // 更多评论...
    ],
    "total": 200,
    "hasMore": true
  }
}
```

#### 5.5.2 添加评论

##### 5.5.2.1 发表歌曲评论

**请求**：

- URL: `/comments/songs/:songId`
- 方法: `POST`
- 数据类型: `application/json`
- 认证: 需要 JWT token

**路径参数**：

```
songId: 歌曲 ID
```

**请求体**：

```json
{
  "content": "这首歌真不错！"
}
```

**响应**：

```json
{
  "code": 0,
  "message": "评论成功",
  "data": {
    "id": 2,
    "content": "这首歌真不错！",
    "userId": 456,
    "username": "当前用户",
    "avatar": "https://example.com/current-avatar.jpg",
    "createdAt": "2022-01-02T12:00:00Z",
    "likesCount": 0
  }
}
```

### 5.6 用户相关 API

#### 5.6.1 获取用户信息

##### 5.6.1.1 获取当前登录用户信息

**请求**：

- URL: `/user/me`
- 方法: `GET`
- 认证: 需要 JWT token

**响应**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "id": 123,
    "username": "user123",
    "nickname": "音乐爱好者",
    "avatar": "https://example.com/avatar.jpg",
    "bio": "热爱音乐的程序员",
    "createdAt": "2022-01-01T00:00:00Z"
  }
}
```

#### 5.6.2 更新用户信息

##### 5.6.2.1 更新当前用户资料

**请求**：

- URL: `/user/me`
- 方法: `PUT`
- 数据类型: `application/json`
- 认证: 需要 JWT token

**请求体**：

```json
{
  "nickname": "新昵称",
  "bio": "更新个人简介"
}
```

**响应**：

```json
{
  "code": 0,
  "message": "更新成功",
  "data": {
    "id": 123,
    "username": "user123",
    "nickname": "新昵称",
    "avatar": "https://example.com/avatar.jpg",
    "bio": "更新个人简介",
    "createdAt": "2022-01-01T00:00:00Z"
  }
}
```

### 5.7 收藏相关 API

#### 5.7.1 获取收藏列表

##### 5.7.1.1 获取用户收藏的歌曲

**请求**：

- URL: `/user/favorites`
- 方法: `GET`
- 认证: 需要 JWT token

**查询参数**：

```
limit: 每页数量，默认 20
offset: 偏移量，默认 0
```

**响应**：

```json
{
  "code": 0,
  "message": "success",
  "data": {
    "songs": [
      {
        "id": 1,
        "title": "收藏的歌曲标题",
        "artist": "艺术家",
        "album": "专辑名称",
        "coverUrl": "https://example.com/cover.jpg",
        "duration": 240,
        "addedAt": "2022-01-01T12:00:00Z"
      }
      // 更多收藏歌曲...
    ],
    "total": 50,
    "hasMore": false
  }
}
```

#### 5.7.2 添加到收藏

##### 5.7.2.1 将歌曲添加到收藏

**请求**：

- URL: `/user/favorites`
- 方法: `POST`
- 数据类型: `application/json`
- 认证: 需要 JWT token

**请求体**：

```json
{
  "songId": 1
}
```

**响应**：

```json
{
  "code": 0,
  "message": "添加收藏成功"
}
```

#### 5.7.3 从收藏中删除

##### 5.7.3.1 从收藏列表中删除歌曲

**请求**：

- URL: `/user/favorites/:songId`
- 方法: `DELETE`
- 认证: 需要 JWT token

**路径参数**：

```
songId: 歌曲 ID
```

**响应**：

```json
{
  "code": 0,
  "message": "取消收藏成功"
}
```

## 6. 代码风格和最佳实践

### 6.1 基本编码规范

#### 6.1.1 JavaScript/TypeScript 代码风格

- **缩进**：使用 2 个空格进行缩进，不使用制表符
- **分号**：语句结尾必须使用分号
- **变量命名**：
  - 驼峰命名法（camelCase）用于变量、函数、方法名
  - 帕斯卡命名法（PascalCase）用于类、组件名称
  - 全大写加下划线（UPPER_CASE）用于常量
- **字符串**：优先使用单引号 `'`，模板字符串使用反引号 `` ` ``
- **括号**：
  - `if`、`else`、`for`、`while`、`function` 等语句的主体必须用大括号包裹
  - 函数参数列表、对象字面量、数组字面量的开始括号前不使用空格，结束括号后不使用空格
- **空行**：
  - 在逻辑块之间使用空行分隔
  - 文件末尾保留一个空行

**示例：**

```javascript
// 好的做法
const USER_NAME = "admin";
let userCount = 0;

function formatDate(date) {
  if (!date) {
    return "";
  }

  // 逻辑块之间有空行
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, "0");
  const day = String(date.getDate()).padStart(2, "0");

  return `${year}-${month}-${day}`;
}

// 类名使用 PascalCase
class UserService {
  // 方法使用 camelCase
  async getUserInfo(userId) {
    // 实现代码
  }
}
```

#### 6.1.2 Vue 3 代码风格

- **组件名称**：
  - 使用 PascalCase 命名组件（如 `MusicPlayer`、`SideBar`）
  - 文件名与组件名保持一致，使用 PascalCase 或 kebab-case
- **模板风格**：
  - 组件模板中的属性值使用双引号
  - 自闭合标签使用自闭合语法（如 `<my-component />`）
  - 多个属性时，每个属性单独一行
- **指令简写**：
  - 使用 `:` 代替 `v-bind:`
  - 使用 `@` 代替 `v-on:`
  - 使用 `#` 代替 `v-slot:`

**示例：**

```vue
<template>
  <!-- 好的做法 -->
  <div class="music-player">
    <audio
      :src="currentSong.url"
      :autoplay="isPlaying"
      @ended="handleSongEnd"
    />

    <button class="play-button" @click="togglePlay" :disabled="!currentSong">
      {{ isPlaying ? "暂停" : "播放" }}
    </button>
  </div>
</template>

<script>
export default {
  name: "MusicPlayer", // PascalCase 命名
  // ...
};
</script>
```

### 6.2 组件设计原则

#### 6.2.1 组件化思想

- **单一职责**：每个组件只负责一个功能领域，避免功能过于复杂
- **可复用性**：设计通用组件，减少代码重复
- **可组合性**：通过组件嵌套和 props/event 通信实现功能组合
- **封装性**：内部实现细节对外不可见，只通过接口（props/events）与外部交互

#### 6.2.2 组件结构组织

- **组件拆分**：根据功能将界面拆分为合理的组件层级
- **props 设计**：
  - 定义清晰的 props，包括类型、默认值和验证
  - 避免 props 嵌套过深
  - 使用 kebab-case 命名模板中的 props
- **事件处理**：
  - 使用 kebab-case 命名自定义事件
  - 事件名称使用动词或动词短语

**示例：**

```vue
<script>
export default {
  name: "SongItem",
  props: {
    // 清晰定义 props 类型和默认值
    song: {
      type: Object,
      required: true,
      validator: (value) => {
        return (
          value &&
          typeof value.id === "number" &&
          typeof value.title === "string"
        );
      },
    },
    isActive: {
      type: Boolean,
      default: false,
    },
  },
  emits: ["play", "add-to-playlist"], // 声明自定义事件
  methods: {
    handlePlay() {
      this.$emit("play", this.song);
    },
    handleAddToPlaylist() {
      this.$emit("add-to-playlist", this.song);
    },
  },
};
</script>
```

### 6.3 Vue 3 Composition API 最佳实践

#### 6.3.1 setup 函数规范

- **导入顺序**：
  1. 导入第三方库
  2. 导入 Vue 组合式函数
  3. 导入自定义 hooks
  4. 导入组件
- **响应式数据创建**：
  - 使用 `ref()` 创建基本类型响应式数据
  - 使用 `reactive()` 创建对象类型响应式数据
  - 避免直接修改 props，使用 `toRefs` 或计算属性
- **逻辑组织**：将相关逻辑组织到可复用的组合式函数中

**示例：**

```javascript
// 组件中的 setup 函数
import { ref, computed, onMounted, watch } from "vue";
import { useRouter, useRoute } from "vue-router";
import { usePlayerStore } from "@/stores/player";
import SongList from "@/components/SongList.vue";

export default {
  components: {
    SongList,
  },
  setup(props) {
    const router = useRouter();
    const route = useRoute();
    const playerStore = usePlayerStore();

    const loading = ref(false);
    const error = ref("");
    const songs = ref([]);

    // 计算属性
    const hasSongs = computed(() => songs.value.length > 0);

    // 方法
    const fetchSongs = async () => {
      loading.value = true;
      try {
        // 获取数据逻辑
        songs.value = await getSongs();
        error.value = "";
      } catch (err) {
        error.value = "获取歌曲列表失败";
        console.error(err);
      } finally {
        loading.value = false;
      }
    };

    // 生命周期
    onMounted(() => {
      fetchSongs();
    });

    // 监听
    watch(
      () => route.params.categoryId,
      (newId) => {
        fetchSongs();
      }
    );

    return {
      loading,
      error,
      songs,
      hasSongs,
      fetchSongs,
    };
  },
};
```

#### 6.3.2 自定义 Composition API (Hooks)

- **命名规范**：使用 `use` 前缀命名自定义 hooks（如 `usePlayer`、`useAuth`）
- **职责单一**：每个 hook 专注于一个功能领域
- **返回值**：返回相关的响应式状态、方法和计算属性

**示例：**

```javascript
// usePlayer.js - 自定义 hook 示例
import { ref, computed, watch } from "vue";
import { useStore } from "vuex";

export function usePlayer() {
  const store = useStore();

  // 从 store 获取状态
  const currentSong = computed(() => store.state.player.currentSong);
  const isPlaying = computed(() => store.state.player.isPlaying);

  // 本地状态
  const progress = ref(0);

  // 方法
  const play = (song) => {
    store.dispatch("player/play", song);
  };

  const pause = () => {
    store.dispatch("player/pause");
  };

  const togglePlay = () => {
    store.dispatch("player/togglePlay");
  };

  // 监听播放状态变化
  watch(isPlaying, (newVal) => {
    console.log("播放状态改变:", newVal);
  });

  return {
    currentSong,
    isPlaying,
    progress,
    play,
    pause,
    togglePlay,
  };
}
```

### 6.4 状态管理最佳实践

#### 6.4.1 Vuex/Pinia 使用规范

- **模块划分**：根据功能模块拆分 store（如 player、user、settings）
- **状态设计**：
  - 保持状态扁平化，避免嵌套过深
  - 状态只包含必要的原始数据，派生数据使用 getter
- **Mutation/Action 规范**：
  - Mutation 名称使用全大写加下划线（如 `SET_CURRENT_SONG`）
  - Action 名称使用 camelCase（如 `fetchUserInfo`）
  - Mutation 必须是同步函数
  - 异步操作应放在 Action 中
- **命名空间**：使用命名空间隔离不同模块的状态

**示例 (Vuex):**

```javascript
// store/modules/player.js
const state = {
  currentSong: null,
  isPlaying: false,
  playlist: [],
  currentIndex: -1,
};

const mutations = {
  // 全大写命名
  SET_CURRENT_SONG(state, song) {
    state.currentSong = song;
  },

  SET_PLAYING_STATE(state, isPlaying) {
    state.isPlaying = isPlaying;
  },

  SET_PLAYLIST(state, playlist) {
    state.playlist = playlist;
  },
};

const actions = {
  // camelCase 命名
  async playSong({ commit, dispatch }, song) {
    commit("SET_CURRENT_SONG", song);
    commit("SET_PLAYING_STATE", true);

    // 异步操作
    await dispatch("updatePlayHistory", song);
  },

  togglePlay({ commit, state }) {
    commit("SET_PLAYING_STATE", !state.isPlaying);
  },
};

const getters = {
  // 派生数据
  isFirstSong: (state) => state.currentIndex === 0,
  isLastSong: (state) => state.currentIndex === state.playlist.length - 1,
};

export default {
  namespaced: true,
  state,
  mutations,
  actions,
  getters,
};
```

#### 6.4.2 状态管理选择

- **小范围状态**：使用组件的 `ref` 和 `reactive`
- **组件树共享状态**：使用 `provide/inject`
- **全局状态**：使用 Vuex/Pinia
- **URL 相关状态**：使用 Vue Router 的路由参数和 query

### 6.5 性能优化建议

#### 6.5.1 组件优化

- **使用 `v-show` vs `v-if`**：
  - 频繁切换显示的元素使用 `v-show`
  - 条件渲染且不频繁切换的元素使用 `v-if`
- **列表渲染优化**：
  - 始终为 `v-for` 提供唯一的 `key` 属性
  - 大型列表使用虚拟滚动
- **组件拆分**：避免大型组件，合理拆分功能

**示例：**

```vue
<!-- 好的做法 -->
<ul>
  <li 
    v-for="song in songs" 
    :key="song.id"  <!-- 使用唯一的 key -->
  >
    {{ song.title }} - {{ song.artist }}
  </li>
</ul>

<!-- 频繁切换显示的内容使用 v-show -->
<div v-show="isPlaying" class="now-playing">
  当前正在播放: {{ currentSong.title }}
</div>

<!-- 条件渲染使用 v-if -->
<div v-if="user.isLoggedIn">
  <user-profile :user="user" />
</div>
<login-form v-else />
```

#### 6.5.2 响应式优化

- **使用 `shallowRef` 和 `shallowReactive`**：对于不需要深度响应式的大型对象
- **使用 `markRaw`**：对于不需要响应式的第三方库对象
- **避免在模板中使用复杂表达式**：将复杂计算移至计算属性
- **使用 `v-once`**：对于不需要重新渲染的静态内容

#### 6.5.3 网络请求优化

- **数据缓存**：缓存频繁请求的数据
- **延迟加载**：非首屏内容使用懒加载
- **请求合并**：避免短时间内发送大量相同请求
- **错误重试**：对关键请求实现适当的重试机制
- **请求超时**：设置合理的请求超时时间

#### 6.5.4 构建优化

- **代码分割**：使用动态导入实现路由懒加载
- **Tree-shaking**：确保使用 ES 模块语法，避免副作用
- **资源压缩**：确保构建过程中压缩 JS、CSS 和图片
- **按需引入**：使用 UI 库时按需引入组件

**示例（路由懒加载）：**

```javascript
// router/index.js
const routes = [
  {
    path: "/",
    name: "Home",
    component: () => import("@/views/HomeView.vue"), // 动态导入
  },
  {
    path: "/about",
    name: "About",
    // 使用动态导入并命名 chunk
    component: () =>
      import(/* webpackChunkName: "about" */ "@/views/AboutView.vue"),
  },
];
```

### 6.6 可访问性（Accessibility）最佳实践

- **语义化 HTML**：使用适当的 HTML 元素（如 `<button>`, `<nav>`, `<main>` 等）
- **ARIA 属性**：为自定义组件添加适当的 ARIA 角色和属性
- **键盘导航**：确保所有交互元素可通过键盘访问
- **颜色对比度**：确保文本与背景的颜色对比度符合 WCAG 标准
- **标签和提示**：为表单元素提供明确的标签和错误提示

**示例：**

```vue
<!-- 语义化 HTML 和 ARIA 属性 -->
<nav aria-label="主导航">
  <ul>
    <li><a href="/">首页</a></li>
    <li><a href="/discover">发现音乐</a></li>
    <li><a href="/my">我的音乐</a></li>
  </ul>
</nav>

<!-- 键盘可访问的自定义按钮 -->
<div
  role="button"
  tabindex="0"
  @click="playSong"
  @keydown.enter="playSong"
  @keydown.space="playSong"
>播放</div>

<!-- 表单标签 -->
<div class="form-group">
  <label for="search-input">搜索音乐</label>
  <input 
    id="search-input"
    type="text" 
    v-model="searchQuery"
    aria-describedby="search-hint"
  />
  <small id="search-hint">输入歌曲名或艺术家</small>
</div>
```

### 6.7 注释规范

- **组件文档**：为公共组件添加组件文档，说明用途、props、events 等
- **代码注释**：为复杂逻辑添加解释性注释
- **函数注释**：为重要函数添加 JSDoc 风格注释
- **TODO 注释**：使用 `// TODO: 描述` 标记待完成工作
- **临时注释**：使用 `// TEMP: 描述` 标记临时代码

**示例：**

```javascript
/**
 * 播放指定歌曲
 * @param {Object} song - 歌曲对象
 * @param {number} song.id - 歌曲 ID
 * @param {string} song.title - 歌曲标题
 * @param {string} song.url - 歌曲播放链接
 * @returns {Promise<void>}
 */
async function playSong(song) {
  if (!song || !song.url) {
    console.error("无效的歌曲对象");
    return;
  }

  try {
    // 设置当前歌曲
    currentSong.value = song;

    // TODO: 添加歌曲到播放历史
    // await addToHistory(song);

    // 播放逻辑
    await audioElement.play();
  } catch (error) {
    console.error("播放失败:", error);
    throw error;
  }
}
```

## 7. 常见故障排除场景和解决方案

### 7.1 开发环境问题

#### 7.1.1 Node.js 依赖问题

**问题描述**：安装依赖时出现错误，或者项目无法正常启动。

**解决方案**：

1. 确保使用正确版本的 Node.js（建议 v16.x 或更高版本）：

   ```bash
   node -v
   ```

2. 清除 npm 缓存并重试安装：

   ```bash
   npm cache clean --force
   rm -rf node_modules
   rm package-lock.json
   npm install
   ```

3. 如果特定包安装失败，可以尝试单独安装或使用 yarn：
   ```bash
   npm install 特定包名
   # 或者
   yarn install
   ```

#### 7.1.2 开发服务器启动失败

**问题描述**：运行 `npm run dev` 时开发服务器无法启动或报错。

**解决方案**：

1. 检查端口是否被占用：

   ```bash
   # Windows
   netstat -ano | findstr :3000
   # macOS/Linux
   lsof -i :3000
   ```

2. 更改开发服务器端口：在 `vite.config.js` 中修改端口配置

3. 检查错误日志，根据具体错误信息进行修复

### 7.2 Vue 3 相关问题

#### 7.2.1 响应式数据不更新

**问题描述**：修改数据后，UI 没有相应更新。

**解决方案**：

1. 确保正确使用响应式 API：

   - 使用 `ref()` 定义基本类型数据
   - 使用 `reactive()` 定义对象或数组
   - 对于数组，避免使用索引直接修改，可以使用 `push`、`splice` 等方法

2. 检查是否正确解构响应式对象：
   ```javascript
   // 错误示例
   const { count } = reactive({ count: 0 });
   // 正确示例
   const state = reactive({ count: 0 });
   // 或者使用 toRefs
   const { count } = toRefs(reactive({ count: 0 }));
   ```

#### 7.2.2 计算属性不生效

**问题描述**：计算属性没有根据依赖变化而更新。

**解决方案**：

1. 确保计算属性只依赖于响应式数据
2. 避免在计算属性中执行异步操作或有副作用的操作
3. 检查计算属性的依赖是否在函数内部正确引用

#### 7.2.3 组件通信问题

**问题描述**：父子组件或跨组件通信失效。

**解决方案**：

1. 对于父子组件：检查 props 是否正确传递，事件是否正确触发和监听
2. 对于跨组件：考虑使用 provide/inject 或 Pinia/Vuex
3. 检查组件是否正确挂载，事件名称是否匹配

### 7.3 Vue Router 问题

#### 7.3.1 路由跳转失败

**问题描述**：使用 `router.push()` 或 `<router-link>` 时页面不跳转。

**解决方案**：

1. 检查路由配置是否正确，路径是否存在
2. 检查组件是否正确导出
3. 检查是否有路由守卫阻止了跳转

#### 7.3.2 路由参数获取问题

**问题描述**：无法正确获取路由参数。

**解决方案**：

1. 检查路由配置中是否正确定义了参数：

   ```javascript
   { path: '/song/:id', component: SongDetail }
   ```

2. 正确获取参数：
   ```javascript
   // 在组件中
   const route = useRoute();
   // 方法1：直接获取
   const songId = route.params.id;
   // 方法2：响应式获取
   const songId = computed(() => route.params.id);
   ```

### 7.4 状态管理问题

#### 7.4.1 Vuex/Pinia 使用问题

**问题描述**：状态更新后组件不刷新，或无法正确访问状态。

**解决方案**：

1. 对于 Vuex：确保使用 `commit` 或 `dispatch` 更新状态，不要直接修改 state
2. 对于 Pinia：确保 store 实例正确创建和使用
3. 检查组件是否正确引入和使用 store

### 7.5 API 请求问题

#### 7.5.1 网络错误

**问题描述**：API 请求失败，出现网络错误。

**解决方案**：

1. 检查网络连接是否正常
2. 验证 API 地址是否正确
3. 检查请求头是否符合要求，特别是认证相关的头部
4. 使用浏览器开发者工具查看网络请求详情

#### 7.5.2 认证错误

**问题描述**：API 请求返回认证失败错误。

**解决方案**：

1. 检查 token 是否有效，是否过期
2. 确保请求时正确携带认证信息
3. 如果使用拦截器，检查拦截器逻辑是否正确

### 7.6 性能问题

#### 7.6.1 组件渲染性能

**问题描述**：页面加载缓慢，组件渲染卡顿。

**解决方案**：

1. 使用 `v-if` 和 `v-show` 合理控制组件的渲染
2. 使用组件懒加载：
   ```javascript
   const SongDetail = () => import("../views/SongDetail.vue");
   ```
3. 使用虚拟列表处理长列表：
   ```javascript
   // 可以使用 vue-virtual-scroller 等库
   npm install vue-virtual-scroller
   ```

#### 7.6.2 内存泄漏

**问题描述**：长时间使用后，应用内存占用持续增加。

**解决方案**：

1. 清理定时器和事件监听器：
   ```javascript
   onUnmounted(() => {
     clearInterval(timer);
     window.removeEventListener("resize", handleResize);
   });
   ```
2. 避免循环引用
3. 正确清理异步操作：

   ```javascript
   const abortController = new AbortController();

   onUnmounted(() => {
     abortController.abort();
   });
   ```

### 7.7 浏览器兼容性问题

**问题描述**：在某些浏览器中功能不正常或样式显示错误。

**解决方案**：

1. 检查浏览器兼容性，使用 Babel 进行转译
2. 对于 CSS 兼容性问题，可以使用 autoprefixer
3. 针对特定浏览器的问题，可以添加特定的 CSS 前缀或 JavaScript 兼容代码

## 8. 贡献指南

### 8.1 参与项目的方式

感谢您对 QQ 音乐播放器项目的关注和支持！我们欢迎各种形式的贡献，包括但不限于：

- **代码贡献**：修复 bug、实现新功能、优化性能等
- **文档改进**：完善技术文档、添加使用示例等
- **问题反馈**：报告 bug、提出功能建议等
- **测试**：参与测试，发现和报告问题

### 8.2 如何开始贡献

#### 8.2.1 环境准备

1. 确保您已按照「环境设置指南」完成本地开发环境配置
2. Fork 项目仓库到您的 GitHub 账户
3. 克隆您的 Fork 到本地
   ```bash
   git clone https://github.com/您的用户名/qq-music-player.git
   cd qq-music-player
   ```
4. 添加原仓库作为上游仓库
   ```bash
   git remote add upstream https://github.com/原作者/qq-music-player.git
   ```

#### 8.2.2 选择或创建 Issue

在开始开发前，请先查看或创建相关的 Issue：

1. 浏览现有 Issue，查找您感兴趣的任务
2. 如果没有找到合适的 Issue，可以创建一个新的 Issue 描述您想实现的功能或修复的 bug
3. 在 Issue 中说明您的计划，获取项目维护者的反馈和建议

### 8.3 开发流程

#### 8.3.1 创建分支

基于最新的 develop 分支创建您的功能分支：

```bash
# 确保您的本地 develop 分支是最新的
git checkout develop
git pull upstream develop

# 创建新分支
# 对于功能开发: feature/功能描述
# 对于错误修复: fix/错误描述
# 例如:
# git checkout -b feature/add-dark-mode
# git checkout -b fix/player-controls-bug

```

#### 8.3.2 代码开发

在开发过程中，请遵循以下原则：

- 遵循项目的代码风格和最佳实践（参见第 6 章）
- 编写清晰的代码注释
- 为新功能添加适当的测试
- 确保您的代码不会破坏现有功能

#### 8.3.3 提交更改

提交您的更改时，请遵循提交规范：

````bash
# 查看更改
git status

# 添加更改
# 建议只添加必要的文件，而不是使用 git add .
git add 文件路径

# 提交更改，使用规范的提交信息
git commit -m "类型[(可选范围)]: 描述"

# 例如:
git commit -m "feat(player): 添加播放速度控制功能"
git commit -m "fix(sidebar): 修复侧边栏在移动设备上无法正常显示的问题"
g```

#### 8.3.4 更新分支

在提交 Pull Request 之前，请确保您的分支与最新的上游 develop 分支保持同步：

```bash
# 切换到 develop 分支
git checkout develop

# 拉取上游更新
git pull upstream develop

# 切换回您的功能分支
git checkout your-feature-branch

# 合并 develop 分支到您的功能分支
git merge develop

# 解决可能出现的冲突
````

#### 8.3.5 推送更改

将您的更改推送到您的 Fork 仓库：

```bash
git push origin your-feature-branch
```

### 8.4 提交 Pull Request

1. 在 GitHub 上导航到您的 Fork 仓库
2. 点击 "New Pull Request" 按钮
3. 选择您的功能分支和原仓库的 develop 分支
4. 填写 Pull Request 的标题和描述：
   - 标题简明扼要地描述您的更改
   - 描述中详细说明您的更改内容、解决问题和实现方法
   - 引用相关的 Issue（例如 "Fixes #123"）
5. 点击 "Create Pull Request" 提交

### 8.5 代码审查流程

代码审查是保证项目质量的重要环节，请耐心配合审查者的反馈。

#### 8.5.1 代码审查原则

- **尊重与建设性**：保持尊重和专业的态度，提供建设性的反馈
- **聚焦于代码**：讨论代码本身，而不是开发者
- **清晰明了**：提供具体、明确的反馈，说明问题和改进建议

#### 8.5.2 审查者职责

1. 检查代码是否符合项目的代码风格和最佳实践
2. 验证功能是否按预期工作
3. 检查是否有潜在的性能问题或错误
4. 确认代码是否有适当的注释和文档
5. 提供建设性的反馈和改进建议

#### 8.5.3 开发者职责

1. 对审查者的反馈做出及时回应
2. 根据反馈进行必要的修改
3. 解释您的实现思路和决策理由
4. 确保所有测试通过

#### 8.5.4 常见的审查关注点

- **代码质量**：代码是否清晰、简洁、可维护
- **功能完整性**：是否完全实现了需求，有没有遗漏的边缘情况
- **错误处理**：是否有适当的错误处理和异常捕获
- **性能**：是否有性能优化的空间
- **安全性**：是否存在潜在的安全问题
- **测试覆盖率**：是否有足够的测试确保功能正常工作

### 8.6 常见问题解答

#### 8.6.1 我的 Pull Request 何时会被审核？

我们会尽快审核所有的 Pull Request，但具体时间可能会根据项目维护者的工作负载而有所不同。一般情况下，我们会在 1-3 个工作日内给出初步反馈。

#### 8.6.2 我的 Pull Request 被拒绝了，怎么办？

不要灰心！代码审查的目的是提高代码质量，而不是批评。请仔细阅读审查者的反馈，理解他们的关注点，然后进行必要的修改。如果有任何疑问，可以在 Pull Request 中进行讨论。

#### 8.6.3 我可以参与项目的其他方面吗？

当然可以！除了代码贡献，您还可以：

- 帮助回答其他开发者的问题
- 参与功能讨论和设计决策
- 完善文档和使用指南
- 报告和验证 bug

### 8.7 行为准则

我们致力于为所有贡献者提供一个友好、包容和尊重的环境。请遵循以下行为准则：

- 尊重所有参与者，无论其背景、经验水平、性别、性别认同和表达、性取向、障碍、外表、种族、年龄、宗教或国籍
- 接受建设性的批评，优雅地提出自己的意见
- 专注于对社区最有利的事情
- 对其他社区成员表示同理心

任何形式的骚扰、歧视或不当行为都是不可接受的。违反行为准则的参与者可能会被警告或从项目中移除。

### 8.8 联系我们

如果您有任何问题或需要帮助，可以通过以下方式联系项目维护者：

- 在 GitHub 上创建 Issue
- 参与项目的讨论

感谢您的贡献和支持！

## 9. 文档整合与索引

### 9.1 文档目录

以下是本文档的完整目录结构，便于快速定位和访问相关内容：

1. [项目概述和目标](#1-项目概述和目标)

   - 1.1 项目简介
   - 1.2 主要功能
   - 1.3 项目目标
   - 1.4 技术栈

2. [系统架构和组件关系](#2-系统架构和组件关系)

   - 2.1 整体架构
   - 2.2 核心组件结构
   - 2.3 导航系统设计
   - 2.4 数据流
   - 2.5 关键交互流程

3. [环境设置指南](#3-环境设置指南)

   - 3.1 先决条件
   - 3.2 项目安装步骤
   - 3.3 配置要求
   - 3.4 常见问题解决

4. [开发工作流程指南](#4-开发工作流程指南)

   - 4.1 分支管理策略
   - 4.2 提交规范
   - 4.3 日常开发流程
   - 4.4 代码审查流程

5. [API 规范文档](#5-api-规范文档)

   - 5.1 API 基础信息
   - 5.2 认证机制
   - 5.3 音乐相关 API
   - 5.4 音乐馆 API
   - 5.5 评论 API
   - 5.6 用户相关 API
   - 5.7 收藏相关 API

6. [代码风格和最佳实践](#6-代码风格和最佳实践)

   - 6.1 基本编码规范
   - 6.2 组件设计原则
   - 6.3 Vue 3 Composition API 最佳实践
   - 6.4 状态管理最佳实践
   - 6.5 性能优化建议
   - 6.6 可访问性（Accessibility）最佳实践
   - 6.7 注释规范

7. [常见故障排除场景和解决方案](#7-常见故障排除场景和解决方案)

   - 7.1 开发环境问题
   - 7.2 Vue 3 相关问题
   - 7.3 Vue Router 问题
   - 7.4 状态管理问题
   - 7.5 API 请求问题
   - 7.6 性能问题
   - 7.7 浏览器兼容性问题

8. [贡献指南](#8-贡献指南)

   - 8.1 参与项目的方式
   - 8.2 如何开始贡献
   - 8.3 开发流程
   - 8.4 提交 Pull Request
   - 8.5 代码审查流程
   - 8.6 常见问题解答
   - 8.7 行为准则
   - 8.8 联系我们

9. [文档整合与索引](#9-文档整合与索引)
   - 9.1 文档目录
   - 9.2 关键术语表
   - 9.3 文档版本信息
   - 9.4 如何使用本文档

### 9.2 关键术语表

| 术语            | 解释                                                                               |
| --------------- | ---------------------------------------------------------------------------------- |
| SPA             | 单页应用（Single Page Application），一种在单个页面加载所有必要资源的 Web 应用架构 |
| Vue 3           | 渐进式 JavaScript 框架，用于构建用户界面                                           |
| Composition API | Vue 3 中新的 API 风格，提供更灵活的组件逻辑组织方式                                |
| Vue Router      | Vue.js 官方路由管理器，用于构建单页应用的路由系统                                  |
| Pinia           | Vue 官方状态管理库，Vuex 的继任者，提供更简洁的 API                                |
| Vite            | 下一代前端构建工具，提供快速的开发服务器启动和热模块替换                           |
| JWT             | JSON Web Token，一种用于安全传输信息的开放标准                                     |
| Git Flow        | 一种 Git 分支管理工作流，定义了不同类型分支的用途和生命周期                        |
| Pull Request    | 代码审查机制，用于将一个分支的更改合并到另一个分支                                 |
| ARIA            | Web 可访问性倡议（WAI-ARIA），提供使 Web 内容和 Web 应用更易于访问的方法           |

### 9.3 文档版本信息

- **版本**：1.0.0
- **最后更新**：2023 年 10 月
- **文档状态**：正式版

### 9.4 如何使用本文档

本技术文档旨在帮助开发者快速了解和参与 QQ 音乐播放器项目的开发。根据您的需求，可以按照以下方式使用本文档：

#### 9.4.1 新开发者入职

1. 首先阅读[项目概述和目标](#1-项目概述和目标)，了解项目的整体情况
2. 查看[系统架构和组件关系](#2-系统架构和组件关系)，熟悉项目结构
3. 按照[环境设置指南](#3-环境设置指南)配置开发环境
4. 学习[开发工作流程指南](#4-开发工作流程指南)，了解团队的开发规范

#### 9.4.2 功能开发参考

1. 查阅[API 规范文档](#5-api-规范文档)，了解可用的后端接口
2. 参考[代码风格和最佳实践](#6-代码风格和最佳实践)，确保代码质量
3. 在遇到问题时，查看[常见故障排除场景和解决方案](#7-常见故障排除场景和解决方案)

#### 9.4.3 参与项目贡献

1. 阅读[贡献指南](#8-贡献指南)，了解如何参与项目
2. 按照指南创建 Issue 和 Pull Request
3. 积极参与代码审查，提供和接收反馈
