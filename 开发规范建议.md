# 开发规范建议

## 1. 代码风格规范

### 1.1 TypeScript规范
- 使用强类型，避免使用 `any` 类型
- 为所有函数参数和返回值指定类型
- 使用接口定义复杂对象类型
- 优先使用类型推断，但在复杂场景下显式声明类型

### 1.2 ESLint规范
- 项目已配置ESLint，确保遵循配置的规则
- 使用 `npm run lint` 检查代码质量
- IDE中安装ESLint插件，实时检测代码问题

### 1.3 Vue组件风格
- 使用Composition API进行组件开发
- 组件命名遵循PascalCase格式
- 使用 `<script setup>` 语法糖简化组件开发
- 模板中使用kebab-case的组件名

```vue
<!-- 正确示例 -->
<template>
  <div class="music-player">
    <player-control :is-playing="isPlaying" @toggle="handleToggle" />
  </div>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import PlayerControl from './components/PlayerControl.vue'

const isPlaying = ref(false)
const handleToggle = () => {
  isPlaying.value = !isPlaying.value
}
</script>
```

## 2. 命名约定

### 2.1 目录和文件命名
- 目录名使用kebab-case（短横线分隔）
- 组件文件名使用PascalCase
- 工具函数文件使用camelCase
- 常量文件使用UPPER_CASE

```
components/
  MusicPlayer.vue
  SongList.vue
utils/
  formatTime.ts
  api.ts
constants/
  API_ENDPOINTS.ts
```

### 2.2 变量和函数命名
- 变量名使用camelCase
- 常量使用UPPER_CASE
- 函数名使用camelCase
- 组件内部状态以业务含义命名

```typescript
// 正确示例
const currentSong = ref(null)
const playDuration = ref(0)
const MAX_PLAYLIST_SIZE = 100

const formatDuration = (seconds: number): string => {
  // 实现逻辑
}
```

## 3. 组件开发规范

### 3.1 组件设计原则
- 组件应该职责单一，遵循单一职责原则
- 通用组件应放在 `components/common` 目录下
- 业务组件放在 `components/business` 目录下
- 复杂组件可以拆分多个子组件，放在同名文件夹中

### 3.2 Props和Emits定义
- 使用TypeScript接口定义Props类型
- 为Props设置默认值和验证规则
- Emits使用对象形式定义，指定事件参数类型

```typescript
import { defineProps, defineEmits } from 'vue'

interface Song {
  id: number
  name: string
  artist: string
}

const props = defineProps<{
  song: Song
  isActive?: boolean
}>()

const emit = defineEmits<{
  play: [songId: number]
  like: [songId: number]
}>()
```

## 4. 状态管理规范

### 4.1 Pinia Store使用
- 按照功能模块划分store
- 使用TypeScript定义store的state类型
- Actions处理异步操作
- Getters用于派生状态

```typescript
// stores/player.ts
import { defineStore } from 'pinia'
import { ref, computed } from 'vue'
import { useSongUrl, useSongDetail } from '@/utils/api'

export interface Song {
  id: number
  name: string
  artists: Array<{name: string}>
  album: {name: string}
  duration: number
}

export const usePlayerStore = defineStore('player', () => {
  const currentSong = ref<Song | null>(null)
  const isPlaying = ref(false)
  
  const playSong = async (songId: number) => {
    try {
      // 获取歌曲详情
      const detail = await useSongDetail(songId)
      currentSong.value = detail
      isPlaying.value = true
    } catch (error) {
      console.error('播放歌曲失败:', error)
    }
  }
  
  return {
    currentSong,
    isPlaying,
    playSong
  }
})
```

## 5. 路由配置规范

### 5.1 路由定义
- 使用TypeScript定义路由参数类型
- 为路由添加meta信息（标题、权限等）
- 懒加载路由组件优化性能

```typescript
// router/index.ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/',
      name: 'home',
      component: () => import('@/views/HomeView.vue'),
      meta: { title: '首页' }
    },
    {
      path: '/playlist/:id',
      name: 'playlist',
      component: () => import('@/views/PlayListView.vue'),
      props: true,
      meta: { title: '歌单详情' }
    }
  ]
})

// 动态设置页面标题
router.afterEach((to) => {
  document.title = to.meta.title as string || 'QQ音乐播放器'
})

export default router
```

## 6. API调用规范

### 6.1 API函数设计
- API函数统一放在 `utils/api.ts` 中
- 使用函数式API设计，避免class
- 每个API函数负责单一接口调用
- 统一错误处理和响应转换

```typescript
// utils/api.ts
import axios from 'axios'

// 获取歌曲详情
export const useSongDetail = async (id: number) => {
  try {
    const res = await axios.get(`/api/song/detail?ids=${id}`)
    if (!res.data || !res.data.songs || res.data.songs.length === 0) {
      throw new Error('歌曲信息获取失败')
    }
    
    const songData = res.data.songs[0]
    return {
      id: songData.id,
      name: songData.name,
      artists: songData.ar,
      album: songData.al,
      duration: songData.dt / 1000 // 转换为秒
    }
  } catch (error) {
    console.error('获取歌曲详情失败:', error)
    throw error
  }
}
```

## 7. 提交信息格式

### 7.1 提交信息结构
- 使用语义化的提交信息格式
- 提交信息应简明扼要地描述更改内容
- 建议使用以下前缀标识提交类型：
  - `feat`: 添加新功能
  - `fix`: 修复bug
  - `docs`: 文档更新
  - `style`: 代码风格修改（不影响功能）
  - `refactor`: 代码重构
  - `perf`: 性能优化
  - `test`: 测试相关
  - `chore`: 构建过程或辅助工具变动

```
feat(playback): 添加播放进度条拖动功能
fix(api): 修复歌曲搜索接口错误处理
refactor(player): 重构播放器组件结构
```

## 8. 项目结构维护规范

### 8.1 目录结构维护
- 遵循当前的目录结构约定
- 新增文件时保持一致的命名规则
- 及时清理未使用的代码和文件
- 大型组件按功能拆分到单独目录

### 8.2 资源管理
- 静态资源放在 `assets` 目录下
- 图片资源使用合适的格式和大小
- 对于较大的媒体文件，考虑使用CDN或懒加载

## 9. 测试规范

### 9.1 单元测试
- 使用Vitest进行单元测试
- 测试文件与被测试文件放在同一目录，命名为 `*.test.ts`
- 确保组件和工具函数都有适当的测试覆盖率
- 运行 `npm run test:unit` 执行测试

```typescript
// utils/formatTime.test.ts
import { describe, it, expect } from 'vitest'
import { formatDuration } from './formatTime'

describe('formatDuration', () => {
  it('should format seconds correctly', () => {
    expect(formatDuration(60)).toBe('01:00')
    expect(formatDuration(125)).toBe('02:05')
    expect(formatDuration(3600)).toBe('60:00')
  })
})
```

## 10. 代码审查规范

### 10.1 审查要点
- 代码是否符合项目规范
- 是否存在潜在的性能问题
- 错误处理是否完善
- 代码可读性和可维护性
- 测试覆盖率是否足够

### 10.2 审查流程
- 提交代码前先运行lint检查
- 创建Pull Request时提供清晰的描述
- 至少一位团队成员审查通过后才能合并
- 审查意见应具体、建设性